import { Header } from "./Layout.styled";
import { Length } from "../features/length/Length";

export const NavBar = () => {
  return (
    <>
      <Header>
        <Length />
      </Header>
    </>
  );
};
